{# templates/menu/components/_meta_card.html.twig #}


    <div class="col-6">
        <div class="menu-show__meta-card">
            <span class="small text-muted">
                {{ title|default('Pas de titre enregistré') }}
            </span>
            <div class="fw-semibold">
                {{ value|default('Pas de valeur enregistrée') }}
            </div>
        </div>
    </div>

