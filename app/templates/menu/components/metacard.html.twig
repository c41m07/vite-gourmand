{# templates/menu/components/metcard.html.twig #}


    <div class="col-6">
        <div class="menu-show__meta-card">
            <span class="small text-muted">
                {{ title|default('pas de titre enregistré') }}
            </span>
            <div class="fw-semibold">
                {{ value|default('pas de valeur enregistré') }}
            </div>
        </div>
    </div>

