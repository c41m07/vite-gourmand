{# templates/partials/_footer.html.twig #}
<footer class="border-top bg-dark mt-5 text-light vg-footer">
    <div class="container py-4">
        <div class="row g-4 align-items-start">
            <div class="col-12 col-md-4 text-start">
                <h2 class="h6 fw-bold">Horaires</h2>
                <ul class="list-unstyled mb-0">
{#                    {% for day, slots in openingHours %}#}
{#                        <li>{{ day }}:#}
{#                        {%  for slot in slots %}#}
{#                        {{ slot.opensAt }} - {{ slot.closesAt }}#}
{#                        {% if not loop.last %} / {% endif %}#}
{#                        {% endfor %}#}
{#                        </li>#}
{#                    {% endfor %}#}
                </ul>
            </div>



            <div class="col-12 col-md-4 text-center">
                <h2 class="h6 fw-bold">Vite & Gourmand</h2>
                <ul class="list-unstyled mb-0">
                    <li> 5 rue de la rue</li>
                    <li> 33000 Bordeaux</li>
                    <li>Téléphone : 05 56 00 00 00</li>
                    <li>Email : contact@vite-gourmand.fr</li>
                </ul>
            </div>

            <div class="col-12 col-md-4 text-start text-lg-end">
                <h2 class="h6 fw-bold">Informations</h2>
                <ul class="list-unstyled mb-0">
                    <li><a class="link-light text-decoration-none" href="{{ path('app_legal') }}">Mentions légales</a></li>
                    <li><a class="link-light text-decoration-none" href="{{ path('app_cgv') }}">CGV</a></li>
                </ul>
                <p class="small mt-4 mb-0">© {{ "now"|date("Y") }} Vite & Gourmand</p>
            </div>
        </div>


    </div>
</footer>
